# Dependencies are declared in pyproject.toml -- this file exists only for
# tooling that expects it. Prefer installing the package itself:
#
#   pip install -e '.[api]'         API service
#   pip install -e '.[app]'         Streamlit UI
#   pip install -e '.[dl]'          PyTorch U-Net
#   pip install -e '.[api,dev]'     everything needed to develop and test
#
# Installing this file alone gives you the base library dependencies without
# registering the `sar_oil_spill` package, so imports will fail. Use `-e .`.

numpy>=1.26
scipy>=1.11
opencv-python-headless>=4.9
scikit-image>=0.22
scikit-learn>=1.4
matplotlib>=3.8
pillow>=10.2
pyyaml>=6.0
